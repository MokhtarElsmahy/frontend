export interface MedadLibVM {
    locations: locations[];
}

export interface locations {
    id: string;
    name: string;
    discoveryDisplayName: string;
    campusId: string;
    isActive: string;
}

